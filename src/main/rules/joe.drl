package spacesim

import spacesim.Ship

rule "Start Moving"
	when
		s:Ship(speed==0, thrust==false)
	then 
		s.thrust=true;
		update(s);
end

rule "Kill Throttle At Max Speed"
	when
		s:Ship(speed>=10, thrust==true)
	then
		s.thrust=false;
		update(s);
end