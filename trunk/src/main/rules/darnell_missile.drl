package spacesim

import spacesim.Missile

rule "Detonate"
	when
		m:Missile(ed<explosionRadius, boom==false)
	then 
		m.boom=true;
		update(m);
end

rule "Follow"
	when
		m:Missile(angle!=ea)
	then
		m.angle=m.ea;
		update(m);
end