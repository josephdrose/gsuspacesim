package spacesim

import spacesim.Ship

rule "Start Moving"
	when
		s:Ship(dx==0, forwardThrust==false)
	then 
		s.forwardThrust=true;
		s.angle-=20;
		update(s);
end

rule "Fire When Ready Grimley"
	when
		s:Ship(ed<100, fireMissile==false)
	then
		s.fireMissile=true;
		update(s);
end